@model IEnumerable<DAL.Security_Logins_Log>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Source_IP)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Logon_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Is_Succesful)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Security_Logins.Login)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Source_IP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Logon_Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Is_Succesful)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Security_Logins.Login)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
