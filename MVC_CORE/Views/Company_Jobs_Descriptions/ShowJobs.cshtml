@model IEnumerable<DAL.Company_Jobs_Descriptions>

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Position
        </th>
        <th>
            Short Description
        </th>
        <th>
            Aply 
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.Company_Jobs.Id)
        </th>
        <th></th>*@
    </tr>
    @*-----------------------------------------------------------------------------------*@

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Job_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Job_Descriptions)
            </td>
             <td>
                 @Html.ActionLink("Aply", "AplyForJob", new { id=item.Id })
            </td>
        </tr>
    }

</table>

